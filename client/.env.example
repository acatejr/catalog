# API Configuration
# The base URL for the Catalog API
# In development, this uses the Vite proxy configured in vite.config.ts
VITE_API_BASE_URL=/api

# Your API key for authenticating with the Catalog API
# Replace this with your actual API key from the server
VITE_API_KEY=your-api-key-here
