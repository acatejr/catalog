# from django.conf.urls import url
from django.urls import path, include

# from .views import (
#     DocumentListApiView,
#     DocumentListView
# )

# urlpatterns = [
#     path('api', DocumentListApiView.as_view()),
#     path('api/documents/<str:search_term>/', DocumentListView.as_view()),
# ]
